# Packages that require full disk cleanup during build
# One package name per line, comments start with #
# These packages typically have large build dependencies or require more disk space

ayugram-desktop

